version: '3.8'

services:
  portfolio:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "8000:80"
    container_name: portfolio-frontend
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    networks:
      - portfolio-network 

networks:
  portfolio-network:
    driver: bridge